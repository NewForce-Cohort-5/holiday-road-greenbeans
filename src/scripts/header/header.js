export const Header = () => {
    const header = document.querySelector('header');
    header.innerHTML += `
    <header class="header"
    <div>
    <img class="header-image" src="images/image.png" alt="Greenbean Logo">
    <div/>
  </header>
    `
}